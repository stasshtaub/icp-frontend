const trigger = document.querySelector(".comments__more-btn");

if (trigger) {
    trigger.addEventListener("click", loadComments)
}

const loadComments = () => {
    // TODO: Реализовать загрузку комментов, когда появится на беке
}