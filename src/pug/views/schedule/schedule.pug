extends ../../layouts/default

block styles
    link(rel="stylesheet" href!=`${baseDir}css/main.css?v=${Date.now()}`)
    link(rel="stylesheet" href!=`${baseDir}css/schedule.css?v=${Date.now()}`)
block content
    .schedule
       include ./particles/schedule-header
       include ./particles/schedule-chips
       include ../capabilities/particles/capabilities-schedule
block scripts
    script(src!=`${baseDir}js/common.js?v=${Date.now()}`)
    script(src!=`${baseDir}js/schedule.js?v=${Date.now()}`)
