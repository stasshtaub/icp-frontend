extends ../../layouts/default

block styles
    link(rel="stylesheet" href!=`${baseDir}css/main.css?v=${Date.now()}`)
    link(rel="stylesheet" href!=`${baseDir}css/programs.css?v=${Date.now()}`)
block content
    .programs
        .container
            include particles/programs-header
            include particles/programs-content
       
block scripts
    script(src!=`${baseDir}js/common.js?v=${Date.now()}`)
    script(defer src!=`${baseDir}js/programs.js?v=${Date.now()}`)
